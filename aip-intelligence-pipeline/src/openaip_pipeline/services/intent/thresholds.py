"""
Default thresholds for semantic intent classification.

DEFAULT_MIN_TOP1 is the minimum cosine similarity required for the top-scoring intent.
DEFAULT_MIN_MARGIN is the required difference between the top1 and top2 scores.
"""

DEFAULT_MIN_TOP1 = 0.58
DEFAULT_MIN_MARGIN = 0.06
