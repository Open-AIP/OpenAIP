Extract ONLY AIP project rows from this page.
Return fields exactly:
aip_ref_code, program_project_description, implementing_agency, start_date, completion_date,
expected_output, source_of_funds, personal_services, maintenance_and_other_operating_expenses,
financial_expenses, capital_outlay, total, errors, category.

Rules:
- Output only real project entries.
- Exclude headers, subtotals, totals, grand totals, and notes.
- Keep strict column mapping.
- errors must be null.
- category must be null.
- If no project table exists, return an empty projects array.

