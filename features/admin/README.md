# Admin Feature

Template: `route-shell`

Contains admin route-facing subfeatures (`dashboard`, `aip-monitoring`, `feedback-moderation`, `usage-controls`, etc.).
Shared contracts and mappers must stay in `lib/**`.
